func fib
t -> t
n -> b {
  b = 1
  let mut a = 1
  let mut index = n - 2
  while index > 0 {
    defer index = index - 1
    let next = a + b
    a = b
    b = next
  }
}

func fibSlow
t -> t
n -> b {
  let temp : t = fib(45)
  if n <= 2 {
    b = 1
    return
  }
  b = fibSlow(n-1) + fibSlow(n-2)
}

func main
-> rType
-> ret {
  let blub : I64 = fib(45)
  ret = fibSlow(45)
}
